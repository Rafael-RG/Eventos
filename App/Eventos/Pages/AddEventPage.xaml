<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage
    x:Class="Eventos.Pages.AddEventPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:extensions="clr-namespace:Eventos.Extensions"
    xmlns:local="clr-namespace:Eventos.Converters"
    xmlns:pages="clr-namespace:Eventos.Common.Pages;assembly=Eventos.Common"
    xmlns:viewModels="clr-namespace:Eventos.ViewModels"
    x:DataType="viewModels:AddEventViewModel"
    x:TypeArguments="viewModels:AddEventViewModel"
    Shell.NavBarIsVisible="false">

    <ContentPage.Resources>
        <ResourceDictionary>
            <local:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout BackgroundColor="#184159">
        <StackLayout BackgroundColor="#DDDDDD">
            <Border
                Margin="0,-10,0,0"
                Padding="20"
                BackgroundColor="#184159"
                Stroke="#184159"
                StrokeThickness="1">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="0,0,30,30" />
                </Border.StrokeShape>
                <Image Source="recuerdame_white" />
            </Border>
        </StackLayout>

        <StackLayout
            Margin="0,0,0,0"
            Padding="20,10,20,10"
            BackgroundColor="#DDDDDD"
            HorizontalOptions="FillAndExpand"
            Spacing="15"
            VerticalOptions="FillAndExpand">
            <ActivityIndicator
                IsRunning="{Binding IsBusy}"
                IsVisible="{Binding IsBusy}"
                VerticalOptions="CenterAndExpand"
                Color="#ABBA3C" />
            <StackLayout IsVisible="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}" VerticalOptions="FillAndExpand">
                <Frame BackgroundColor="White" CornerRadius="20">
                    <Frame.Shadow>
                        <Shadow
                            Brush="#000000"
                            Opacity="0.5"
                            Radius="4"
                            Offset="0,3" />
                    </Frame.Shadow>
                    <StackLayout>
                        <Label
                            Margin="0,0,0,9"
                            FontSize="Title"
                            Text="Crear nuevo evento"
                            TextColor="#ABBA3C" />

                        <!--  Título del evento  -->
                        <Label
                            FontSize="12"
                            Text="Título"
                            TextColor="#184159" />
                        <Entry
                            Margin="0,0,0,9"
                            Placeholder="Ingrese el título del evento"
                            PlaceholderColor="#666666"
                            Text="{Binding Title}"
                            TextColor="#666666" />

                        <!--  URL del evento  -->
                        <Label
                            FontSize="12"
                            Text="URL del evento"
                            TextColor="#184159" />
                        <Entry
                            Margin="0,0,0,9"
                            Placeholder="Ingrese el url del evento"
                            PlaceholderColor="#666666"
                            Text="{Binding Url}"
                            TextColor="#666666" />

                        <!--  Primer alarma del evento  -->
                        <Label
                            Margin="0,0,0,9"
                            FontSize="12"
                            Text="Alarma 1"
                            TextColor="#184159" />
                        <Picker
                            Title="Seleccione una alarma"
                            ItemsSource="{Binding Alarms}"
                            SelectedItem="{Binding SelectedFirstAlarm}"
                            TextColor="#666666"
                            TitleColor="#666666" />

                        <!--  Segunda alarma del evento  -->
                        <Label
                            FontSize="12"
                            Text="Alarma 2"
                            TextColor="#184159" />
                        <Picker
                            Title="Seleccione una alarma"
                            Margin="0,0,0,9"
                            ItemsSource="{Binding Alarms}"
                            SelectedItem="{Binding SelectedSecondAlarm}"
                            TextColor="#666666"
                            TitleColor="#666666" />

                        <!--  Fecha del evento  -->
                        <Label
                            FontSize="12"
                            Text="Fecha"
                            TextColor="#184159" />
                        <DatePicker
                            Margin="0,0,0,9"
                            Date="{Binding Date}"
                            Format="dd/MM/yyyy"
                            TextColor="#666666" />

                        <!--  Zona horaria del evento  -->
                        <Label
                            FontSize="12"
                            Text="Zona Horaria"
                            TextColor="#184159" />
                        <Picker
                            Title="Seleccione una zona horaria"
                            Margin="0,0,0,9"
                            ItemsSource="{Binding Zones}"
                            SelectedItem="{Binding SelectedZone}"
                            TextColor="#666666"
                            TitleColor="#666666" />
                        <StackLayout
                            HorizontalOptions="FillAndExpand"
                            Orientation="Horizontal"
                            Spacing="70">
                            <StackLayout>
                                <!--  Hora de inicio del evento  -->
                                <Label
                                    FontSize="12"
                                    Text="Hora de inicio"
                                    TextColor="#184159" />
                                <TimePicker TextColor="#666666" Time="{Binding StartTime, Mode=TwoWay}" />
                            </StackLayout>
                            <StackLayout>
                                <!--  Hora de fin del evento  -->
                                <Label
                                    FontSize="12"
                                    Text="Hora de fin"
                                    TextColor="#184159" />
                                <TimePicker TextColor="#666666" Time="{Binding EndTime, Mode=TwoWay}" />
                            </StackLayout>
                        </StackLayout>
                    </StackLayout>
                </Frame>

                <!--  Botón para agregar el evento  -->
                <StackLayout VerticalOptions="EndAndExpand">
                    <Button
                        Margin="0,20,0,9"
                        BackgroundColor="#419387"
                        Command="{Binding AddEventCommand}"
                        CornerRadius="20"
                        Text="Guardar evento"
                        TextColor="White"
                        VerticalOptions="EndAndExpand">
                        <Button.Shadow>
                            <Shadow
                                Brush="#000000"
                                Opacity="0.5"
                                Radius="4"
                                Offset="0,3" />
                        </Button.Shadow>
                    </Button>

                    <Button
                        BackgroundColor="#D2E898"
                        Command="{Binding ClearCommand}"
                        CornerRadius="20"
                        Text="Cancelar"
                        TextColor="#205048">
                        <Button.Shadow>
                            <Shadow
                                Brush="#000000"
                                Opacity="0.5"
                                Radius="4"
                                Offset="0,3" />
                        </Button.Shadow>
                    </Button>
                </StackLayout>

            </StackLayout>
        </StackLayout>
    </StackLayout>

</pages:BaseContentPage>
